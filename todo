- implement a_teammate's scripts and code
- fix up menus (linux only ?)
- update, add and remove some commands
  - disable:
	    - /rate
  - add:
	    - /_Graphox_protocol
	    - /_Graphox_news
- fix server sorting (how'd that break ??)
- start offline chat
- start server mod (linked, anti-spam, commands, geoip, inter-chat (admins only), inter-msg (all, only if registered with friends) server status (#servstats <ip>; online (how long), offline (how long)))